```mermaid
mindmap
  root((Sprint 04: Threading & Task Orchestration))
    Goal
      "Centralize all background task execution under TaskOrchestrator"
      "Integrate batch/parallel logic from scripts/mvp_app.py"
      "Refactor UI to delegate work via orchestrator API"
      "Reduce UI load/task latency by 30%"
    In_Progress
      "Integration of batch/parallel logic"
      "Refactor of legacy code"
    Next
      "Enterprise features, API endpoints, advanced analytics (planned for future)"
``` 